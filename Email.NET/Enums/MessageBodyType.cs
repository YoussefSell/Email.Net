namespace Email.NET
{
    /// <summary>
    /// the type of the message body
    /// </summary>
    public enum MessageBodyType
    {
        /// <summary>
        /// the message body is a plain text
        /// </summary>
        PlainText,

        /// <summary>
        /// the message body is HTML
        /// </summary>
        Html
    }
}
